import { CustomIngredientStatistics } from './custom-ingredient-statistics';

describe('CustomIngredientStatistics', () => {
  it('should create an instance', () => {
    expect(new CustomIngredientStatistics()).toBeTruthy();
  });
});
