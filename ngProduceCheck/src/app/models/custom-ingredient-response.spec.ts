import { CustomIngredientResponse } from './custom-ingredient-response';

describe('CustomIngredientResponse', () => {
  it('should create an instance', () => {
    expect(new CustomIngredientResponse()).toBeTruthy();
  });
});
