import { SelectedUserRecipesPipe } from './selected-user-recipes.pipe';

describe('SelectedUserRecipesPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedUserRecipesPipe();
    expect(pipe).toBeTruthy();
  });
});
