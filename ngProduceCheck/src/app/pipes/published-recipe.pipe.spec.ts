import { PublishedRecipePipe } from './published-recipe.pipe';

describe('PublishedRecipePipe', () => {
  it('create an instance', () => {
    const pipe = new PublishedRecipePipe();
    expect(pipe).toBeTruthy();
  });
});
